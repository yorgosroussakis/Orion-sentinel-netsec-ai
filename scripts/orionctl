#!/usr/bin/env python3
"""
Wrapper script for orionctl CLI.

This allows running orionctl directly as a script.
"""

import sys
from orion_ai.cli.orionctl import main

if __name__ == "__main__":
    sys.exit(main())
